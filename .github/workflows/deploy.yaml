name: Deploy MultiDocker
on:
  push:
    branches:
      - multi_container # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t shashanksinghal1994/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true shashanksinghal1994/react-test npm test

      - run: docker build -t shashanksinghal1994/multi-client ./client
      - run: docker build -t shashanksinghal1994/multi-nginx ./nginx
      - run: docker build -t shashanksinghal1994/multi-server ./server
      - run: docker build -t shashanksinghal1994/multi-worker ./worker

      - run: docker push shashanksinghal1994/multi-client
      - run: docker push shashanksinghal1994/multi-nginx
      - run: docker push shashanksinghal1994/multi-server
      - run: docker push shashanksinghal1994/multi-worker
